import Link from "next/link";
import { ArrowLeft, CalendarDays, Clock3, MapPin } from "lucide-react";
const games = [["Today · 8:00 PM","USA","Argentina","Group Stage","Your match"],["Today · 11:00 PM","Brazil","Japan","Group Stage","Next at MetLife"],["Mon, Jun 15 · 5:00 PM","France","Nigeria","Group Stage","MetLife Stadium"]];
export default function SchedulePage() { return <main className="min-h-dvh bg-[#081225] p-5 text-slate-100"><div className="mx-auto max-w-2xl"><Link href="/fan" className="flex items-center gap-2 text-sm text-slate-300"><ArrowLeft className="h-4 w-4" /> Back to matchday</Link><p className="mt-10 text-xs font-semibold uppercase tracking-[.16em] text-primary">Live match schedule</p><h1 className="mt-2 font-display text-3xl font-bold">MetLife Stadium fixtures</h1><p className="mt-2 text-sm text-slate-400">All times shown in local stadium time.</p><div className="mt-6 space-y-3">{games.map(([time,home,away,stage,label],index) => <div key={time} className={`rounded-2xl border p-5 ${index === 0 ? "border-primary/40 bg-primary/[.09]" : "border-white/10 bg-white/[.045]"}`}><div className="flex items-center justify-between"><span className="text-xs font-semibold text-primary">{label}</span><span className="flex items-center gap-1 text-xs text-slate-400"><Clock3 className="h-3.5 w-3.5" />{time}</span></div><p className="mt-4 text-xl font-bold">{home} <span className="mx-2 text-slate-500">vs</span> {away}</p><p className="mt-2 flex items-center gap-1 text-xs text-slate-400"><MapPin className="h-3.5 w-3.5" /> {stage} · MetLife Stadium</p></div>)}</div></div></main>; }
