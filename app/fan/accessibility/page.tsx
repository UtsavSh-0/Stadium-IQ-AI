import Link from "next/link";
import { Accessibility, ArrowLeft, ChevronRight, Headphones, MapPin, MessageCircle } from "lucide-react";

export default function AccessibilityPage() {
  return <main className="min-h-dvh bg-[#081225] p-5 text-slate-100"><div className="mx-auto max-w-2xl"><Link href="/fan" className="flex items-center gap-2 text-sm text-slate-300"><ArrowLeft className="h-4 w-4" /> Back to matchday</Link><div className="mt-10 rounded-3xl border border-white/10 bg-white/[.055] p-6 sm:p-8"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-accent/15 text-accent"><Accessibility className="h-6 w-6" /></div><p className="mt-6 text-xs font-semibold uppercase tracking-[.16em] text-accent">Accessibility assistance</p><h1 className="mt-2 font-display text-3xl font-bold">Move through the stadium your way.</h1><p className="mt-3 text-sm leading-6 text-slate-400">Get step-free directions, sensory-friendly spaces, accessible facilities, or connect with an accessibility team member.</p><div className="mt-6 space-y-3">{[["Step-free route to my seat","Use elevators and ramp-accessible concourses",MapPin,"/fan/directions/seat"],["Find an accessible washroom","Nearest accessible facility · 2 min",Accessibility,"/fan/directions/washroom"],["Request in-person assistance","Accessibility host response within 3 minutes",Headphones,"/fan"]].map(([title,desc,Icon,href]) => { const I = Icon as typeof Accessibility; return <Link href={href as string} key={title as string} className="flex items-center gap-3 rounded-2xl border border-white/10 bg-white/[.04] p-4 hover:bg-white/[.08]"><I className="h-5 w-5 text-primary" /><div className="flex-1"><p className="text-sm font-semibold">{title as string}</p><p className="mt-1 text-xs text-slate-400">{desc as string}</p></div><ChevronRight className="h-4 w-4 text-slate-500" /></Link>})}</div><button className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-accent py-3 text-sm font-bold text-[#071225]"><MessageCircle className="h-4 w-4" /> Chat with accessibility support</button></div></div></main>;
}
