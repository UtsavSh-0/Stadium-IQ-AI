import Link from "next/link";
import { ArrowLeft, Bus, Car, ChevronRight, Clock3, TrainFront } from "lucide-react";
const options = [[TrainFront,"Metro Stadium Link","9 min walk to platform","Trains every 6–8 min"],[Bus,"Official shuttle","Pickup at Transit Hub South","Next shuttle in 11 min"],[Car,"Rideshare pickup","Zone R · West perimeter","Low demand right now"]];
export default function TransportPage() { return <main className="min-h-dvh bg-[#0c0e12] p-5 text-slate-100"><div className="mx-auto max-w-2xl"><Link href="/fan" className="flex items-center gap-2 text-sm text-slate-300"><ArrowLeft className="h-4 w-4" /> Back to matchday</Link><p className="mt-10 text-xs font-semibold uppercase tracking-[.16em] text-primary">After the match</p><h1 className="mt-2 font-display text-3xl font-bold">Choose your way home</h1><p className="mt-2 text-sm text-slate-400">AI recommends the Metro Stadium Link for the fastest exit after the final whistle.</p><div className="mt-6 space-y-3">{options.map(([Icon,title,detail,status]) => { const I=Icon as typeof TrainFront; return <Link key={title as string} href="/fan/directions/gate" className="flex items-center gap-4 rounded-2xl border border-white/10 bg-white/[.05] p-4 hover:bg-white/[.09]"><div className="grid h-11 w-11 place-items-center rounded-xl bg-primary/15 text-primary"><I className="h-5 w-5" /></div><div className="flex-1"><p className="text-sm font-semibold">{title as string}</p><p className="mt-1 text-xs text-slate-400">{detail as string}</p><p className="mt-2 flex items-center gap-1 text-xs text-primary"><Clock3 className="h-3 w-3" /> {status as string}</p></div><ChevronRight className="h-4 w-4 text-slate-500" /></Link>})}</div></div></main>; }
